# Paths auto-stripped from FogAiPrivate before pushing to FogAi (public).
# One path per line. Supports git-filter-repo format (glob:, regex: prefixes).
# All paths stripped recursively.
docs/
doc/
scripts/sync_opensource.sh
inference-services/stream-service-onnx/
